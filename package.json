{
  "name": "worldwidewmal",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "description": "worldwidewmal — creator-led content production for brands",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "publish:pages": "astro build && node scripts/check-links.mjs && node scripts/publish.mjs",
    "check": "node scripts/check-links.mjs"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.0.0",
    "sharp": "^0.34.0"
  }
}
