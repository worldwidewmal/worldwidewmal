services:
  - type: web
    name: ugc-os-dashboard
    runtime: node
    buildCommand: cd dashboard && npm install
    startCommand: cd dashboard && node server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
