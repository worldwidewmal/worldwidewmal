Use the lead-researcher agent to source new Orlando UGC leads only. Do not draft outreach or update pipeline.csv yet.

Steps:
1. Read pipeline.csv to understand which verticals already have leads and which are underrepresented.
2. Read suppression-list.csv so no suppressed companies are returned.
3. Research 5–10 net-new Orlando businesses using live web search. Prioritize verticals with fewer existing pipeline entries.
4. For each lead, locate a verified contact email from an official source. Never guess or construct an email format.
5. If no email is found, log the best fallback route (contact form URL, Instagram handle, LinkedIn, phone).
6. Return all leads in the standard lead record format.

After returning leads, use the qa-crm-operator agent to run a duplicate check against pipeline.csv and suppression-list.csv.

Do not add anything to pipeline.csv without my explicit confirmation after reviewing the leads.
