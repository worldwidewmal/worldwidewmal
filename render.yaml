services:
  - type: web
    name: ugc-os-dashboard
    runtime: node
    rootDir: dashboard
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
